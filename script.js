var main = function (input) {
  var radius = input;
  var pi = 3.1412;
  var area = radius * radius * pi;
  var msg = `A cricle of radius ${radius} units has an area of ${area} units squared`;
  return msg;
};
