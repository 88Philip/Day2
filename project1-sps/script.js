var gameMode = `user to input name`;

var aiChoice = function () {
  var randomDecimal = Math.random() * 3;
  var randomInteger = Math.floor(randomDecimal);

  if (randomInteger === 1);
  return (aiChoice = `Sissors`);

  if (randomInteger === 2) return (aiChoice = `Paper`);

  if (randomInteger === 3) return (aichoice = `Stone`);
};

var main = function (input) {
  var myOutputValue = "hello world";
  return myOutputValue;
};
